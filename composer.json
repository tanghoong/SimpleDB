{
    "name": "tanghoong/simpledb",
    "description": "A lightweight NoSQL JSON Document Storage library for PHP",
    "type": "library",
    "license": "MIT",
    "require": {
        "php": ">=8.2"
    },
    "require-dev": {
        "phpunit/phpunit": "^11.0",
        "phpstan/phpstan": "^1.0"
    },
    "suggest": {
        "ext-pdo_sqlite": "Use SqliteAdapter for WAL-mode SQLite storage with native SQL query push-down",
        "ext-apcu": "Use ApcuCacheAdapter for shared-memory caching across all PHP-FPM workers"
    },
    "autoload": {
        "psr-4": {
            "SimpleDB\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "SimpleDB\\Tests\\": "tests/"
        }
    }
}
